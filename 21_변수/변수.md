# 변수
파일 이름은 소문자로
디렉터리 이름은 모두 소문자로

---
* g_를 사용하여 전역 변수에 접두어를 지정
* s_를 사용하여 정적 변수에 접두사를 지정
* k : 상수 예) kSzzzzz 시작, 첫글자 대문자
* s : 문자열 예) sLzzzzz 시작, 첫글자 대문자
a : array. . 단독으로 쓰이지는 않으며 g, m 을 빼고는 맨 앞에 옴
---cpp
     int anScores[10]; char *asNames[10];(이름 쵀대 길이가 9인 문자열들의 포인터)
---
* b : 1. byte 2. bool 보통 bool 의 용도로 많이 사용함 (byte 단독으로 '이 값은 byte, 즉 unsigned char 변수다' 라고 쓸일이 거의 없음)
   * BOOL bUseTime; BOOL bIsStarted;
* c : 1. count. 2. charactor 3. class 보통 count 의 용도로 많이 사용함 (charactor 단독으로 쓸 일이 거의 없음). 가끔 class 용도로도 사용하는데,
     주로 MFC 에서 control 일 경우에만 그렇게 사용함 (4. control)
     int cChecked 
     CListCtrl m_cListScores;
* ci : class instance. class 이름이 길다거나 적당한 표현식이 없을 때 사용
  *  class myclass ciScores;
* d : double (decimal 로 생각하면 안됨)
* dw : Windows 에서 double word (4 byte 값). 잘 안씀.
* e : -

* f : 1. float 2 flag 3. function 용도가 모호함. 요즘은 float 으로 전용하려함. function 의 경우 단독으로 쓰이는 경우가 없이 항상 포인터 접두어와 같이 쓰이므로 float 과는 구분이 됨 (float *pfAvgScore;void (*pfGetAvgScore)();

* g: global 전역 변수 앞에 붙는 접두어. 맨 앞에 오며 요즘은 뒤에 _ 를 붙여서 사용함.

    int g_nMaxScore;

* h : Windows 프로그램 할 때의 handle
* i : array 혹은 여러개의 요소를 가지는 componet 에 사용하는 index

    int iList;

* j : -
* k : -
* l : long 이라고 생각할 수도 있는데, 안씀.
* li : long integer (int64 8 byte 짜리) . Windows 에 LARGE_INTEGER 라는 구조체가 있긴 한데 통상 long integer
* m : 1. class 의 member 접두어. 맨 앞에 오며 뒤에 _ 를 붙여서 사용함. java 에서는 _를 안붙임
---cpp
    m_nMinScore;
     2. win32 프로그램 할 때 mmresult 값. 요즘은 mmresult 를 거의 쓸일이 없음. MMRESULT mAudioIn;
 ---
* n : 정수 type. int, long, char, short, signed , unsigned 물불 안가리고 왠만한것은 다 이걸 씀. 변수 크기(byte, word...)까지 표현해보려 했으나 그냥 n 으로 하는것이 낫다 판단함
* o : -
* p : 포인터. 포인터의 경우 다 이걸씀. 정수형, 문자열 가리지 않음. 이중 삼중일때 p 를 개수만큼 붙임.

    ---cpp char *pBuffer; ---

* q : Queue? 안씀.
* r ; -
* s : 문자열. 포인터 중에서 문자열로만 쓰일 경우에도 p 대신에 이걸 쓰는 경우도 있고, array 이긴 하지만 a 를 쓰지는 않음
    char sBuffer[10];char *sName;
* st : structure 일 경우 이렇게 표현
    struct totalscore stScore;

* t : time_t 등 시간과 관계된 값들
* u : unsigned . 안씀
* ul : unsigned long 으로 쓰는 경우가 있는데, 거의 안씀
* v : -
* w : -
* x: -
* y : -
* z : MS 에서는 (어떤 헝가리안 표기인지는 모르겠음) null 문자로 끝나는 문자열의 의미로 쓰이는데, 안씀
---
타입인지 : 대문자 ,시작 _없이,첫글자 대문자
변수인지, 
열거형
함수인지, 
---
---
#### 열거형 이름
* N-12. 열거형 이름은 접두어 "e"를 붙이고,
* 각 단어의 첫 글자를 대문자로 쓰며 언더바 "_" 는 사용하지 않는다.
* 열거형 정의 코드 바로 밑에 해당 열거형의 변수 타입 및 이름을 재지정(typedef)하고,
* 코드 상에서는 재 지정된 열거형 이름을 사용한다.

===========================
@ 변수 이름
- 변수 이름은 모두 소문자로 작성
- 사이에 언더바 "_" 를 사용
- 지역변수, 전역변수, 구조체 멤버 변수 모두 동일하다.

예)
int table_name;  // 좋음 - 언더바 사용
int tablename;   // 좋음 - 모두 소문자
int tableName;   // 불가 - 대문자 사용

 ========================
@ 전역변수
- "g_" 와 같이 지역 변수와 구분할 수 있는 접두어를 사용한다.
int g_table_num; 
=========================
@ 함수
- 이름은 대문자로 시작하고 각 단어의 첫 글자를 대문자로 쓰며, 
- 언더바 "_" 는 사용하지 않는다.
예)

AddTableEntry()
DeleteUrl()
​
@ 순서

입력 인자

입출력 인자, 

출력 인자 순서로 정의

========================

@ 상수

 이름은 k로 시작하고, 대소문자가 섞인 형태(매 단어의 첫 글자를 대문자)를 사용한다. 

const int kDaysInAWeek = 7;

​

상수(Constant)
변하지 않고 항상 일정한 불변값
프로그래밍에서는 'const'와 같은 키워드로 선언하여 값의 변경을 막습니다. 
예) const PI = 3.14159

=========================
매크로 이름
N-13. 매크로 정의는 대문자와 언더바 "_"만 사용하고, 값, 수식 등을 정의할 때에는 반드시 괄호를 사용해야 한다.
#define ROUND(x) ...
#define PI_ROUNDED (3.0)

​

=========================
알파벳 순서
// 1) 시스템 라이브러리
#include <sys/types.h>
#include <unistd.h>
​

// 2) 다른 라이브러리
#include <other_libs.h>

​
// 3) 현재 프로젝트
#include "base/basictypes.h"
#include "base/commandlineflags.h"
#include "foo/public/bar.h"





```Cpp
enum eUrlTableErrors 
{ 
  ...
};
```

---

```Cpp
const char *service_name = "PROV_Kitchen";
char deviceName_1[] = "Kitchen Light";
```

```C++
static uint8_t s_변수명 = 1;

```

```C
bool AAA(bool AA) {
  AA = false;
  //AA = true;
  return AA;
}
////////////////////////////////////////////////////////////
int AAA(int AA) {
  if (AA > 100) {
    AA = 1;
  } else {
    AA = 0;
  }
  return AA;
}
```
